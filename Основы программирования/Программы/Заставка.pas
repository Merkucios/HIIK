Program Заставка;
const symbol = '*';  
begin
  // Блок заставки
  writeln(symbol * 38);
  writeln(symbol + 'Программа');
  writeln(symbol);
  writeln(symbol + 'вычисления суммы чисел');
  writeln(symbol);
  writeln(symbol + 'Автор: Медведев Андрей Владимирович');
  writeln(symbol);
  writeln(symbol * 38);
end.